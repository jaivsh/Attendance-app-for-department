class Trip {
  final String title;
  final DateTime startDate;
  final DateTime endDate;
  final String subject;
  final String description;
  final String travelType;

  Trip(this.title, this.startDate, this.endDate, this.subject, this.description,
      this.travelType);
}
